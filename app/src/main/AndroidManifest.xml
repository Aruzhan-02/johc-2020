<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mobile_app_abai">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mobile_app_abai">
        <activity android:name=".Ratingpage"></activity>
        <activity android:name=".Bookspage"></activity>
        <activity android:name=".Process_test" />
        <activity android:name=".userpage" />
        <activity android:name=".Mainpage" />
        <activity android:name=".Login_Form" />
        <activity android:name=".Registration_form" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>